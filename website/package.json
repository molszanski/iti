{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "cloudflare:deploy:preview": "bunx wrangler pages deploy --branch preview --project-name itijs ./dist",
    "cloudflare:deploy:production": "bunx wrangler pages deploy --branch master --project-name itijs ./dist"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.35.2",
    "@catppuccin/starlight": "^1.0.1",
    "astro": "^5.6.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}